<UserControl x:Class="Safe.Dialogs.AddFieldsDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Safe.Dialogs"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <TextBlock Text="Type:" FontSize="30" Margin="5" />
        <ComboBox FontSize="30" Margin="5" SelectedIndex="{Binding Type}">
            <ComboBoxItem Content="Text" />
            <ComboBoxItem Content="Password" />
            <ComboBoxItem Content="Multiline text" />
        </ComboBox>
        <TextBlock Text="Label:" FontSize="30" Margin="5" />
        <TextBox Text="{Binding Label}" FontSize="30" Margin="5" />
        <StackPanel Orientation="Horizontal" Margin="5">
            <Button Content="Add" Command="{Binding AddFieldCommand}" FontSize="20" />
            <Button Content="Cancel" Command="{Binding CancelCommand}" FontSize="20" />
        </StackPanel>
    </StackPanel>
</UserControl>
