<UserControl x:Class="Safe.Views.CreateNewItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel LastChildFill="True">
        <TextBlock Text="Title:" FontSize="30" DockPanel.Dock="Top" Margin="5" />
        <TextBox FontSize="30" Text="{Binding Title}" DockPanel.Dock="Top" Margin="5" />
        <TextBlock Text="Description:" FontSize="30" DockPanel.Dock="Top" Margin="5" />
        <TextBox FontSize="30" Text="{Binding Description}" DockPanel.Dock="Top" Margin="5" />
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
            <Button Content="Create" FontSize="30" Command="{Binding CreateCommand}" />
            <Button Content="Cancel" FontSize="30" Command="{Binding CancelCommand}" />
        </StackPanel>
    </DockPanel>
</UserControl>
