<UserControl x:Class="Safe.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <DockPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch" LastChildFill="True">
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" DockPanel.Dock="Bottom">
            <Button Content="Ok" Command="{Binding OkCommand}" FontSize="16" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" FontSize="16" Margin="5"/>
        </StackPanel>
        <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Auto">
            <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
                <TextBlock Text="Language:" FontSize="20" Margin="5" />
                <TextBox Text="{Binding Language}" FontSize="20" Margin="5" />
                <TextBlock Text="Storage path:" FontSize="20" Margin="5" />
                <TextBox Text="{Binding StoragePath}" FontSize="20" Margin="5" />
            </StackPanel>
        </ScrollViewer>
    </DockPanel>
</UserControl>
